<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sisf">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.Result_Screen"></activity>
        <activity android:name=".Activity.Quiz_screen" />
        <activity
            android:name=".Activity.Home_Quiz"
            android:label="Quiz" />
        <activity
            android:name=".Activity.Forgot_pwd"
            android:label="Forgot Password" />
        <activity
            android:name=".Activity.Login"
            android:label="Login" />
        <activity
            android:name=".Activity.Register"
            android:label="Register" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Activity.map"
            android:label="@string/title_activity_map" />
        <activity android:name=".Activity.Story" />
        <activity android:name=".Activity.Roots_Details" />
        <activity
            android:name=".Activity.FAQ"
            android:label="FAQ" />
        <activity
            android:name=".Activity.Roots"
            android:label="Roots" />
        <activity
            android:name=".Activity.Course"
            android:label="Course" />
        <activity
            android:name=".Activity.ContactUs"
            android:label="Contact Us" />
        <activity
            android:name=".Activity.Home"
            android:label="SISF Education"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity.Splash_screen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>